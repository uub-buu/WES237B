# ARM Neon Block Matrix Multiplication
The starter code in this folder is a CPU-based matrix multiply.  Implement it using block matrix multiplication with ARM Neon Intrinsics.  Make sure you update your Makefile appropriately.

## Report
In your report, run `time` and record the result.  This is the CPU SIMD baseline you will compare your OpenCL implementations against.
